/**
 * Otoplus SOURCE capabilities — verified via live HTML/JSON-LD audit (Aug 2026).
 * Path: /{brand} or /{brand}/{series}
 * Year/city/price are not verified as query params → MATCHER_ONLY.
 */
export const OTOPLUS_VERIFIED_SOURCE_FIELDS = ["brand", "series"] as const;
